parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'

    App\Service\AiService:
        arguments:
            $apiKey: '%env(GIGACHAT_API_KEY)%'
